name: ci

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-ruby@v2
        with:
          ruby-version: "3.3.4"
      - run: bundle install
      - run: bundle exec standardrb
